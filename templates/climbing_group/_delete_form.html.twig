<form method="post" action="{{ path('climbing_group_delete', {'id': climbing_group.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ climbing_group.id) }}">
    <button class="btn">Delete</button>
</form>
